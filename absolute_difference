		;This	program calculates the absolute difference of the parameters
		;set		inputs
		mov		r1,#34 ;declare first parameter
		mov		r2,#27 ;declare second parameter
		
		;find	which variable is higher
		cmp		r1, r2
		 L7

		L1:
		
		end